// Modules & require
// var Count = require('./counter')
// console.log(Count(["a", "b"]))

//Module pattern

var modulePattern = require('./modulePattern')

console.log(modulePattern.addTwoNumbers(5, 8), modulePattern.arrayCount(["x", "y", "z", "p"]));